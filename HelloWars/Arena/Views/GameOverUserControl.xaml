<UserControl x:Class="Arena.Views.GameOverUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Viewbox>
        <TextBlock Text="GAME OVER" TextAlignment="Center" TextWrapping="Wrap" Foreground="{StaticResource CloseButtonStroke}" 
                       FontWeight="Bold" Width="40" >
            <TextBlock.Effect>
                <DropShadowEffect ShadowDepth="4" Direction="330" Color="Black" Opacity="0.7" BlurRadius="7"/>
            </TextBlock.Effect>
        </TextBlock>
    </Viewbox>
</UserControl>
